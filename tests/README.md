# Tests

Test scope for the MVP:

- data contract validation
- `CityContext` generation checks
- scoring consistency
- scenario-level regression checks
- explanation shape validation

Current smoke tests:

- local scenario runner returns the expected output sections
- transport disruption scenario produces review warnings
- multi-events scenario builds zone-level event influence
- richer fake scenarios remain compatible with the local workflow

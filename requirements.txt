# Dependencies are declared in pyproject.toml; this file installs the project itself
# (editable) with the runtime extra, so a single source of truth stays in pyproject.
#
#   pip install -r requirements.txt
#
# Core deps (strands-agents, pydantic, boto3) come from [project.dependencies].
# bedrock-agentcore comes from the [runtime] extra, needed only to serve the
# AgentCore entrypoint (app/.../main.py).
-e .[runtime]

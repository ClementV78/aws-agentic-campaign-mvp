# Runtime dependencies.
#
# strands-agents carries the model access layer: BedrockModel for the Converse
# transport and Agent.structured_output for the Pydantic contracts declared in
# llm_schemas.py. pydantic ships with it and defines those contracts.
#
# Without a configured provider the application still runs end to end: every LLM step
# degrades to its deterministic heuristic. These packages must be installed all the
# same — unlike before the Strands migration, src/ is no longer dependency-free.
strands-agents>=1.15.0
pydantic>=2.0
boto3>=1.35.0

# Only needed to serve the AgentCore Runtime entrypoint (runtime_app.py). It is
# imported lazily, so the CLI, the pipeline and the test suite run without it.
bedrock-agentcore>=1.9.1
